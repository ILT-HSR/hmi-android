package ch.hsr.ifs.gcs.ui.fragments.needs

import android.app.Activity
import org.osmdroid.views.MapView

class NeedInstructionListener(val activity: Activity, val map: MapView) : NeedInstructionFragment.OnNeedInstructionFragmentListener {

    override fun onNeedInstructionFragmentChanged() {
        // TODO Does nothing so far
    }

}