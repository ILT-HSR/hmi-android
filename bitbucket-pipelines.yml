image: fmorgner/android-sdk:latest

pipelines:
  branches:
    develop:
      - step:
          caches:
            - gradle
          script:
            - bash ./gradlew build
      - step:
          caches:
            - gradle
          script:
            - bash ./gradlew test
